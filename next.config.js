/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  // distDir: "build",
  // unoptimized: true,
  // experimental: {
  //   images: true,
  // },
};

module.exports = nextConfig;
